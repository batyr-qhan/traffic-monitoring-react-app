# Traffic Monitoring React App

Веб-приложение для мониторинга дорожно-транспортных происшествий (ДТП) в городе Астана на интерактивной карте.

## 🚀 Запуск проекта

### Требования
- Node.js 16+ или pnpm 10+
- Git

### Установка зависимостей

```bash
pnpm install
```

### Разработка

```bash
pnpm dev
```

Приложение откроется на `http://localhost:5173`

### Сборка для продакшена

```bash
pnpm build
```

### Превью собранной версии

```bash
pnpm preview
```

### Развертывание на GitHub Pages

```bash
pnpm deploy
```

Сайт будет доступен по адресу: `https://yourusername.github.io/traffic-monitoring-react-app/`

## 🏗️ Архитектура проекта

### Структура папок

```
src/
├── components/          # UI компоненты (если будут добавлены)
├── constants/           # Константы приложения
│   └── api.ts          # URL API endpoints
├── hooks/              # Custom React hooks
│   ├── useMapSetup.ts           # Инициализация карты
│   ├── useTrafficLayer.ts       # Управление слоем ДТП
│   ├── useCityBordersLayer.ts  # Управление слоем границ города
│   ├── useDtpData.ts           # Загрузка данных ДТП
│   └── useCityBorders.ts       # Загрузка данных границ города
├── services/           # Бизнес-логика и API запросы
│   └── api.ts          # Функции для загрузки данных
├── stores/             # MobX хранилища состояния
│   ├── UIStore.ts              # Управление видимостью слоев
│   ├── SelectionStore.ts       # Управление выбранной точкой
│   ├── useStores.ts            # Хук для доступа к хранилищам
│   ├── StoreProvider.tsx       # Provider для контекста
│   └── index.ts                # Экспорты и инициализация
├── App.tsx             # Главный компонент приложения
├── main.tsx            # Точка входа приложения
├── App.css             # Стили приложения
└── index.css           # Глобальные стили
```

### Технологический стек

- **React 19** - библиотека для построения UI
- **TypeScript** - типизация JavaScript
- **Vite** - быстрый bundler
- **MapLibre GL** - интерактивная карта (открытое ПО)
- **React Query** - управление асинхронными данными
- **MobX** - реактивное управление состоянием
- **mobx-react-lite** - интеграция MobX с React

### Архитектурные принципы

#### 1. **Разделение ответственности**
- **Hooks** отвечают за логику работы с картой и данными
- **Stores** управляют глобальным состоянием UI
- **Services** содержат API запросы
- **Components** отвечают только за отображение

#### 2. **Управление состоянием**
- **React Query** - для асинхронных данных (ДТП, границы города)
- **MobX Stores** - для синхронного состояния UI:
  - `UIStore` - видимость слоев (points, clusters, borders)
  - `SelectionStore` - выбранная точка ДТП

#### 3. **Реактивность**
- Компоненты обернуты в `observer()` для реактивности на изменения MobX
- Автоматическое отслеживание зависимостей в хуках

## 📡 Работа с API

### Используемые API

Приложение работает с двумя API endpoints:

#### 1. **API ДТП (Traffic Accidents)**
```
URL: https://gis.kgp.kz/arcgis/rest/services/KPSSU/DTP/FeatureServer/0/query?where=area_code+%3D+%271971%27&...&f=geojson
```

**Описание:**
- Получает данные о дорожно-транспортных происшествиях в Астане
- Возвращает GeoJSON с координатами и информацией о ДТП
- Параметр `area_code=1971` фильтрует данные по коду Астаны

**Формат ответа:**
```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "id": 1,
      "geometry": {
        "type": "Point",
        "coordinates": [71.449, 51.169]
      },
      "properties": {
        "fd1r05p1": "2024-01-29 10:30:00"
      }
    }
  ]
}
```

#### 2. **API Границ города**
```
URL: https://services8.arcgis.com/GyR85gR88mMqIY4t/ArcGIS/rest/services/.../FeatureServer/1/query?where=name_en%3D%27Astana%27&...&f=pgeojson
```

**Описание:**
- Получает административные границы города Астана
- Возвращает GeoJSON с полигоном границ города

### Функции загрузки данных

#### `fetchDtpData()` в `src/services/api.ts`
```typescript
export async function fetchDtpData() {
  const response = await fetch(API_URL);
  return response.json();
}
```

**Использование в компонентах:**
```typescript
const { data: dtpData, isLoading, error } = useDtpData();
```

#### `fetchCityBorders()` в `src/services/api.ts`
```typescript
export async function fetchCityBorders() {
  const response = await fetch(CITY_BORDERS_URL);
  return response.json();
}
```

**Использование в компонентах:**
```typescript
const { data: cityBorders, isLoading, error } = useCityBorders();
```

### Конфигурация запросов

React Query настроен с параметрами:
- **`retry: 2`** - повторяет запрос 2 раза при ошибке
- **`staleTime: 5 * 60 * 1000`** - данные считаются свежими 5 минут

### Обработка ошибок

```typescript
const { data, isLoading, error } = useDtpData();

if (isLoading) {
  // Показываем индикатор загрузки
}

if (error) {
  // Показываем сообщение об ошибке
}
```

## 🗺️ Функциональность карты

### Слои карты

1. **Traffic Accidents Layer** - точки ДТП
   - Зеленые точки - невыбранные ДТП
   - Красные точки - выбранные ДТП
   - Кластеризация при масштабировании

2. **Clusters Layer** - кластеры ДТП
   - Автоматическая группировка при удалении

3. **City Borders Layer** - границы города
   - Полупрозрачный черный контур

### Управление видимостью слоев

Переключатели в шапке приложения:
- **Show Points** - видимость отдельных точек ДТП
- **Show Clusters** - видимость кластеров
- **Show Borders** - видимость границ города

Состояние управляется через `UIStore` в MobX.

### Взаимодействие с картой

- **Клик на точку ДТП** - выделение и показ попапа с информацией
- **Клик на кластер** - приближение карты к кластеру
- **Наведение мыши** - изменение курсора на pointer

## 🔧 Полезные команды

```bash
# Запуск в режиме разработки с hot reload
pnpm dev

# Линтинг кода
pnpm lint

# Сборка для продакшена
pnpm build

# Превью продакшн версии локально
pnpm preview

# Развертывание на GitHub Pages
pnpm deploy
```

## 🚢 Развертывание

### GitHub Pages

Проект уже настроен для автоматического развертывания на GitHub Pages.

Просто выполните:
```bash
pnpm deploy
```

И настройте в GitHub Settings → Pages:
- Выберите ветку `gh-pages`
- Выберите папку `/ (root)`

Сайт будет доступен по адресу: `https://yourusername.github.io/traffic-monitoring-react-app/`

## 📚 Дополнительные ресурсы

- [MapLibre GL документация](https://maplibre.org/maplibre-gl-js/)
- [React Query документация](https://tanstack.com/query/latest)
- [MobX документация](https://mobx.js.org/)
- [Vite документация](https://vitejs.dev/)

## 🤝 Возможные улучшения

- Добавить фильтры по типу ДТП и датам
- Реализовать экспорт данных (CSV, JSON)
- Добавить статистику и аналитику
- Оптимизировать загрузку больших объемов данных
- Добавить тестирование (Jest, React Testing Library)
- Реализовать кэширование данных в localStorage
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...

      // Remove tseslint.configs.recommended and replace with this
      tseslint.configs.recommendedTypeChecked,
      // Alternatively, use this for stricter rules
      tseslint.configs.strictTypeChecked,
      // Optionally, add this for stylistic rules
      tseslint.configs.stylisticTypeChecked,

      // Other configs...
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```

You can also install [eslint-plugin-react-x](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-x) and [eslint-plugin-react-dom](https://github.com/Rel1cx/eslint-react/tree/main/packages/plugins/eslint-plugin-react-dom) for React-specific lint rules:

```js
// eslint.config.js
import reactX from 'eslint-plugin-react-x'
import reactDom from 'eslint-plugin-react-dom'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      // Other configs...
      // Enable lint rules for React
      reactX.configs['recommended-typescript'],
      // Enable lint rules for React DOM
      reactDom.configs.recommended,
    ],
    languageOptions: {
      parserOptions: {
        project: ['./tsconfig.node.json', './tsconfig.app.json'],
        tsconfigRootDir: import.meta.dirname,
      },
      // other options...
    },
  },
])
```
# traffic-monitoring-react-app
